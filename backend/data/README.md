Place the provided `nyka.csv` dataset here. The backend will automatically import it into the SQLite database as a table named `nyka` on startup.

The CSV should have a header row with column names. Example prompts (after import):

- "Show the first 10 rows of the nyka table."
- "What is the average price by category?"
- "List top 5 products by revenue."
